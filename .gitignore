# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
.eggs/
*.egg-info/
*.egg
.installed.cfg

# Virtual environments
venv/
env/
ENV/
.venv/

# Jupyter
.ipynb_checkpoints/
notebooks/Untitled*.ipynb
notebooks/*-checkpoint.ipynb
*.ipynb_meta

# Testing & coverage
.pytest_cache/
.tox/
.coverage
.coverage.*
htmlcov/
.hypothesis/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
desktop.ini

# Secrets / env
.env
.envrc
.env.local
.env.*.local
.env*

# Logs
*.log

# Project-specific — internal working documents (NOT distributed)
private_notes/
docs/execution_packets/

# Project-specific — generated artifacts (rebuilt by scripts)
docs/results.aux
docs/results.log
docs/results.out
docs/results.toc
*.tex.bak

# Data cache — locally fetched EODHD parquet (handled in dataset publication pass)
data/cache/
# Intraday cache (regenerated from EODHD; published CSV is the reproducible artifact)
data/cache/intraday_*.parquet
# LLM prompt/response cache (local only, potentially large)
data/cache/llm/

# Raw data exports (portability CSVs, potentially large)
data/raw/

# Claude Code working directory
.claude/

# Local-only reference notebooks (Hilpisch chapter prototypes, not for public consumption)
docs/handoff/15_deep_learning_cross_sectional_panel_data.ipynb
docs/handoff/16_sequence_models_temporal_deep_learning.ipynb

# Internal working artifacts — kept local, not for public repo
CLAUDE.md
docs/handoff/session_2_to_3.md
docs/validation/
docs/architecture.md
docs/refs.bib

# Archived/superseded notebooks — local-only
notebooks/_archived_*.ipynb

# Jupyter auto-save junk
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Smoke-test execution artifacts (local verification only, not committed)
**/_smoke_*.ipynb

# Ad-hoc one-off scripts — local-only
scripts/_add_*.py

# Claude Code session memory (local-only)
memory/

# Local temporary probe scripts
test_intraday*.py

# Local-only reference notebooks (proof-of-concept HTMLs)
notebooks/_local_*.html
notebooks/_local_*.ipynb
notebooks/*.html

# internal working docs — keep local, not public
docs/design/
docs/research/
docs/handoff/

# RL model weights — kept local, out of public repo (large binaries; results live in diagnostics/figures)
results/rl/n29_ppo/checkpoints/*.pt
results/rl/n1/
results/rl/n2/
results/rl/n3/

# rendered-notebook HTML exports (regenerable; inflated size + skewed GitHub language stats)
results/**/*.html
# write-only DL portfolio weight parquets (notebook 05 outputs; nothing reloads them)
results/notebook_05/weights_*.parquet

# archived RL dev scaffolding (N=1/2/3 experiments) — kept local, not public
notebooks/_rl_dev/
results/rl/cumret_n*.png
results/rl/ensemble_weights.png
results/rl/n2_random_sanity.png
results/rl/training_curves.png
